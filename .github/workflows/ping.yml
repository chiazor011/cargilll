name: Ping Render Site

on:
  schedule:
    - cron: '*/5 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping /api/health
        run: curl -sf https://cargill-institutional.onrender.com/api/health || echo "Ping failed"
